{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "bun",
      "request": "launch",
      "name": "调试（预设输入）",
      "program": "src/index.ts",
      "cwd": "${workspaceFolder}",
      "env": {
        "DEBUG_INPUTS": "[\"src/index.ts 里写了什么？\"]"
      }
    },
    {
      "type": "bun",
      "request": "launch",
      "name": "调试当前文件",
      "program": "${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
