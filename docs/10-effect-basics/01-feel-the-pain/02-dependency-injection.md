# 10.1 依赖注入：第三条路（下）

> [上篇](./01-pain-points.md)列了 7 个痛点，这篇讲解法概念。还是不写 Effect，只建立"我们需要什么"的认知。

## 先别怕这个名字

"依赖注入"（Dependency Injection）这个名词听着吓人，其实大白话就是"工具间"模式。用工地打比方。

## 工地的工具：三种管理办法

想象一个建筑工地，工人干活需要各种工具（锤子、锯子、梯子）。工具有三种管理办法：

### 办法一：每个人自己带工具上班（对应痛点 1-4）

每个工人上班自己扛着锤子、锯子、梯子来。

- 想换把新锤子？得通知每个工人都换一把。
- 工地新进了一种电动锯子？得挨个告诉每个工人"记得带上电动锯子"。
- 这就是我们代码的现状：`index.ts` 和 `agent.tsx` 各自带着一份 tools 数组，加个工具要改两处。

### 办法二：角落放个公共工具箱，谁要谁偷偷去拿（对应痛点 5）

工地角落放个工具箱，不登记、不分配，谁需要谁自己去翻。

- 看起来方便--不用每个人自己带了。
- 但问题来了：新来的工人不知道工具箱在哪（得点进 `import` 才发现）；想测试新锤子时，不知道谁在用旧锤子，没法单独替换；工具箱被人换了锁，所有人突然拿不到工具。
- 这就是我们的 `db`：藏在模块里 import，测试时换不了。

### 办法三：设一个工具管理处（这就是依赖注入）

工地设个工具管理处。工人上岗前登记"我需要锤子和锯子"，管理处把工具直接发到工位上。

- 工人不用自己带（不用手动传参）。
- 工人不用偷偷去角落找（不是隐式全局）。
- 工具清单在管理处统一维护，加新工具登记一次就行。
- 想给某个工人测试新锤子？管理处发个假锤子给他，不影响别人。

**办法三就是依赖注入**：有个统一的地方存放所有共享资源，谁需要就声明一声，自动把东西送过去。

## 套到我们的代码上

我们想要的：

- `config`、`provider`、`tools`、`db` 这些共享资源，在程序启动时造好，放进"容器"
- `runAgentLoop` 需要 provider 和 tools？它声明"我需要这俩"，容器自动给
- 想换 mock provider 做测试？容器发个假的就行，不用改 `runAgentLoop` 的代码
- 加第 7 个工具？在容器里登记一次，所有地方自动能用

**Effect-TS 的 Service + Layer 就是这个"容器"**：
- **Service** = 工人登记的"我需要什么"（声明依赖）
- **Layer** = 管理处的"工具怎么造出来"（提供实现）
- 程序启动时把 Layer 组装好，运行时函数通过 Service 自取，不用层层传参

这是 opencode 解决上面七个痛点的方案，也是阶段 10 剩下几课的主题。

## 本课小结

1. **七个痛点全是依赖管理问题**：要么重复（1-3）、要么啰嗦（4）、要么隐式（5）、要么无保证（6-7）
2. **显式传参与隐式全局都不对**：前者像"每个人自己带工具"，后者像"偷偷用公共工具箱"
3. **依赖注入就是"工具管理处"**：统一存放、声明即取用、可替换。Effect-TS 的 Service/Layer 是它的 TS 实现

本课没有写任何代码--痛点都在现有代码里，打开 `src/index.ts` 和 `src/tui/agent.tsx` 对照着看就能发现。下一课开始学 Effect 基础。

---

下一步：[10.2 Effect 是什么](../02-effect-basics/01-what-is-effect.md)
