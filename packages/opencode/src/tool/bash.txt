执行 shell 命令。

参数：
- command：要执行的 shell 命令（如 "git status"、"dir"、"bun test"）

注意：
- 命令在项目根目录执行
- Windows 上用 PowerShell 执行，Mac/Linux 上用 sh 执行
- 输出包括 stdout、stderr 和退出码
- 命令超时 30 秒后自动终止
- 用于 git、dir、npm 等终端操作，不要用于文件读写（用 read/write/edit）
